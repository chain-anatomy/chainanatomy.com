import styled from "@emotion/styled";

import { COLORS } from "config/styles";

const ContentsTit = styled.div`
  font-family: "AlliancePlattMedium";
  font-size: 32px;
  color: ${COLORS.grayscale02};
`;

export default ContentsTit;
